@model List<EventResult>

<div class="container pb-5 mt-3">
    @for (int i = 0; i < Model.Count(); i++)
    {
        @await Html.PartialAsync("_NewsResultView", Model[i], new ViewDataDictionary(ViewData){{"index", i}});
    }
</div>